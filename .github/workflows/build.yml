name: Build zephyr-3.5 firmware 
on:
  workflow_dispatch:
  push:
    paths:
      - "config/**"
      - build.yaml

jobs:
  build:
    uses: petejohanson/zmk/.github/workflows/build-user-config.yml@core/zephyr-3.5-update
